const {chromium}=require('playwright');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{for(const assist of [true,false]){const page=await browser.newPage({viewport:{width:1024,height:768}});await page.addInitScript(assist=>localStorage.setItem('mudline-tune-v1',JSON.stringify({assist})),assist);await page.goto('http://localhost:3020/assets/games/mudline/index.html');await page.waitForTimeout(600);await page.keyboard.down('ArrowRight');await page.keyboard.down('Space');const samples=[];for(let i=0;i<8;i++){await page.waitForTimeout(300);samples.push(await page.evaluate(()=>{const s=mudline.state();return {angle:s.angle,x:s.x,y:s.y};}));}console.log(JSON.stringify({assist,samples}));await page.close();}}finally{await browser.close();}})();
